# 验证记录

基线记录日期：2026-09-22。运行环境：MATLAB 24.2.0.2773142 (R2024b) Update 2，Windows。当前代码使用基础 MATLAB；没有调用 k-Wave、Field II 或 Verasonics。以下 49 项是当日基线计数；`results/validation/test_results.csv` 已在 2026-09-24 更新为 60 项，最新证据见文末。

## 软件检查

- 全部 **49 项 MATLAB 单元/集成测试通过**，0 失败、0 未完成。原始明细为 `results/validation/test_results.csv`。
- 对全部 `.m` 文件运行 `checkcode(...,'-id')`，**0 条代码分析器消息**，机器记录为 `results/validation/code_analyzer.json`。
- 测试涉及坐标与维度、分区互补性、随机种子隔离、焦点/转向延时符号、面积加权激励归一化、复声压解析极限、线性叠加、零源、积分细化、时序平均、占空比、阈值标定检查、体积/CV、边界截断、接口 JSON、端到端模拟和扫描导出。
- 对独立审查发现的复数几何输入、目标被 ROI 裁切、次级峰被视野边界截断等问题，先重现失败再修复。真实参数模板会拒绝运行。

## 已执行的示例

| 任务 | 实际产物 |
|---|---|
| 1D / matrix × focused / broad / expanded | `results/demo/`：6 组 PNG、MAT、交接 JSON 与 summary.csv |
| fixed_element 下 5 孔径 × 3 模式 × 2 几何 | `results/architectures_linear/`、`results/architectures_matrix/`：共30行 |
| fixed_total 下相同组合 | `results/architectures_linear_fixed_total/`、`results/architectures_matrix_fixed_total/`：共30行 |
| 频率扫描 | `results/sweep/`：1.5、2、2.5、3、3.5 MHz，共5点，源速度假设不变 |
| 稀疏栅瓣 | `results/grating_demo/`：曲线图、采样值及预测/观察方向表 |
| 积分与体素细化 | `results/validation/`：surface_convergence.csv、voxel_convergence.csv、convergence.mat |

所有这些结果都是配置定义的虚拟阵列结果；未作真实 ICE 设备或材料验证。已目视检查示例图的坐标、单位、颜色参考和目标/阈值轮廓。

## 数值收敛证据与未解决的精度边界

线阵聚焦示例在27个近/远轴及偏轴点比较矩形表面细分：

| x×y 积分小片数 / 单元 | 相对最细结果的复声压 L2 误差 |
|---|---:|
| 2×11 | 0.33758% |
| 4×22 | 0.067144% |
| 8×44 | 参考 |

这只支持这组点和参数下的积分精度，不保证所有频率、距离和孔径下都已收敛。

固定物理 ROI 和公共阈值参考，将三轴体素间距同时减半/减至四分之一：

| 体素数 | 目标覆盖率 C | 目标外比例 L |
|---:|---:|---:|
| 46,125 | 0.34013 | 0.044817 |
| 369,000 | 0.43456 | 0.045436 |
| 2,952,000 | 0.38849 | 0.045449 |

**覆盖率尚未显示足够稳定的网格收敛。** 阈值边界的离散采样造成显著变化。默认网格用于快速演示，不能凭这些小数值进行精细的方案优劣排名。后续用于科研定量结论时，应围绕目标与阈值边界进一步细化并报告容差，且需要对每种关键案例单独验证。目标外比例在这组细化下相对稳定，也不能据此推断目标内指标稳定。

## 栅瓣检查

虚拟线阵在2.5 MHz、声速1500 m/s、原始间距0.3 mm、转向20°时，保留交替阵元使有效间距变成0.6 mm。规则阵列相位重复关系给出可见栅瓣方向 −41.146°；远场角度扫描观察到 −41.10° 附近峰值。矩形单元方向性会使场峰偏离理想阵列因子的精确方向。完整阵列在该示例中没有额外可见的相位重复方向，但仍有普通旁瓣。

该角度诊断只验证规则一维孔径的远场机制；没有把随机稀疏峰、矩阵斜向峰或有限距离的每个次级峰都判定为栅瓣。

## 实验端仍待验证

真实几何/曲率/声透镜、频带和电压输出能力、硬件延时单位、接收链、吸收/温升、材料阈值单位与剂量关系均未验证。k-Wave 目前是数据和要求接口，Verasonics 目前是非执行交接契约。没有真实硬件发射或实验水槽数据。

## 2026-09-24 P0-A 增量验证

- 全套 `run_tests`：**60/60 通过，0 失败**；机器记录为已更新的 `results/validation/test_results.csv`。旧 49 项回归与 11 项新增检查同时运行。
- 对 `simulation/+ice`、`config`、`scripts`、`adapters/+iceio`、`tests` 的 **39 个 MATLAB 文件**执行 `checkcode(...,'-id')`，**0 条警告**。
- 合成双阵列与连续分区各运行一次，并输出同一公共相对阈值下的比较表、阈值敏感性与图像；运行 `run_p0_convergence` 检查两级体素与两个点的积分分片敏感性。具体数值、ROI 截断与解释限制见 [升级记录](upgrade_2026-09-24.md)。
